```mermaid
sequenceDiagram
    participant User
    participant Browser
    participant Server

    Note right of User: User types note into text field and clicks "Save"

    User->>Browser: Submit note input
    Note right of Browser: Prevent default form submission (page reload)
    Note right of Browser: Create new note object with content and timestamp

    Browser->>Server: POST https://studies.cs.helsinki.fi/exampleapp/new_note
    activate Server
    Note right of Server: Server saves the note
    Server-->>Browser: 302 Redirect to /exampleapp/notes
    deactivate Server

    Browser->>Server: GET https://studies.cs.helsinki.fi/exampleapp/notes
    activate Server
    Server-->>Browser: Updated HTML with new note
    deactivate Server

    Note right of Browser: Page reloads and new note appears in list
